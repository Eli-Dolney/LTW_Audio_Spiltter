# Core dependencies
streamlit>=1.28.0
librosa>=0.10.0
matplotlib>=3.5.0
plotly>=5.0.0
soundfile>=0.10.0
numpy>=1.21.0
scipy>=1.7.0
pretty_midi>=0.1.11
pydub>=0.25.1

# Audio separation - Demucs (PyTorch, recommended for M-series Macs)
demucs>=4.0.0
torch>=2.0.0
torchaudio>=2.0.0

# Optional: Spleeter (not installed by default)
# pip install spleeter

# Melody: librosa pYIN is used by default (no TensorFlow).
# CREPE is optional and can fail on Apple Silicon:
# crepe>=0.0.12
